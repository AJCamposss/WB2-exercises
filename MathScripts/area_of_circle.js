let radius = 4;
let area = Math.PI * Math.pow(radius, 2)
// console.log(areaOfCircle)

const areOfCircle = radius * area

console.log(" The area of a circle with radius " + radius + " is " + area.toFixed(2))